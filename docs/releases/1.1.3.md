# 1.1.3

Standalone README and security pins.

- Store Update/Install only installs the `approvedCommitSha` in `store/index.json`
- Layer self-update is opt-in (default off). Watcher only repairs the ChatGPT patch
- Privileged IPC is gated to ChatGPT/Layer frames
- README credits Codex++ (based on, MIT); repo left the GitHub fork network
- CI: keep `node:crypto` out of the preload bundle; watcher tests match repair-only
- Runtime version is LightHaru/chatgpt-layer 1.1.3
