# 1.1.4

Patch release: installer/formula retarget, fail-closed lockfile, runtime split, and compatibility layer.

- Homebrew formula and installer README point at LightHaru/chatgpt-layer
- Custom update-repo fallback defaults to LightHaru/chatgpt-layer
- Installer lockfile is fail-closed (`npm ci` only)
- Installer one-liners pin downloaded source to tag `v1.1.4`
- Runtime `main.ts` split into focused modules (PR #4)
- Watcher-health: Layer self-update opt-in (default off) and label rename (PR #5)
- Capability-driven runtime compatibility layer (PR #6)
- Runtime version is LightHaru/chatgpt-layer 1.1.4
